using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Director : MonoBehaviour
{
    //credits to spawn
    //reference to the current stage
    //a bool to tell when the player can teleport / stage over

    //a bool to tell when spawn

    //array reference to the enemys
    //array reference to cost : enemy[0] = cost[0]

    //create a random int for, how many enemies, which enemies, and time to next wave
}
